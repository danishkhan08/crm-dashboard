Simple React app. The frontend assumes the backend API is served from the same origin under `/api`.
During development you can set up a proxy in package.json or run both servers with a reverse proxy.
